This folder should contain the minimal MAD repo code needed at runtime:
- models/
- util/
- mad_dataset_utils.py

These files are vendored during build or copied locally before building the Docker image.

